\usepackage[
      backend = biber,
        style = numeric,
   giveninits = false,
      sorting = nyt,
          doi = true,
          url = true,
  maxbibnames = 99,
  minbibnames = 99,
     hyperref = true
]{biblatex}

\addbibresource{references/references.bib}
